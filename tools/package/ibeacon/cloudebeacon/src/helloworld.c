#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
int main()
{
	//char *install = "cd /usr/seentech/netguard/env_install && chmod +x install_lib_env.sh && ./install_lib_env.sh";
	//char *run_sh = "cd /usr/seentech/netguard && ./run.sh";

	//system(install);
	//system(run_sh);
	sleep(5);
	return 0;
}
